

<secion class="library">
	<%- include('../partials/search') %>
	<ul id="songList">
	<% songs.forEach(song => { %>
	<li class="song-item">
		<p class="title"><span>Назва </span><%= song.title %></p>
		<p class="artist"><span>Виконавець </span><%= song.artist %></p>
		<p class="album"><span>Альбом </span><%= song.album %></p>
		<p class="duration"><span>Тривалість </span><%= song.duration %> minutes</p>
		<audio controls name="<%= song.title %>">
			<source src="<%= song.filePath %>" type="audio/mpeg" />
			Your browser does not support the audio tag.
		</audio>
		<a href="./music/<%= song.filename %>" download="<%= song.filename %>">Завантажити</a>
	</li>
	<% }); %>
</ul>
</secion>

